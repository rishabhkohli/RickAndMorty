query GetCharacter($id: ID!) {
    character(id: $id) {
        name
        gender
        species
        status
        type
        image
        origin {
            id
            name
        }
        location {
            id
            name
        }
    }
}